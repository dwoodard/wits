{
  "version": "0.1.0",
  "configurations": [
    {
      "name": "Attach",
      "type": "chrome",
      "request": "attach",
      "port": 9222,
      "url": "localhost",
      "webRoot": "${workspaceRoot}"
    },
    {
      "name": "Attach to url with files served from ./out",
      "type": "chrome",
      "request": "attach",
      "port": 9222,
      "url": "localhost",
      "webRoot": "${workspaceRoot}"
    }
  ]
}
